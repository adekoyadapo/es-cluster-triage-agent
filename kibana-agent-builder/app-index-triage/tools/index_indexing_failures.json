{
  "configuration": {
    "params": {
      "cluster_name": {
        "description": "Optional cluster name filter; use an empty string to query all clusters.",
        "type": "string"
      },
      "index_pattern": {
        "description": "Index or data stream pattern to investigate (e.g. logs-app.payments-*, .ds-metrics-k8s-*). Required.",
        "type": "string"
      },
      "lookback": {
        "description": "Lookback window as ES|QL time span text (e.g. 15 minutes, 1 hour, 24 hours).",
        "type": "string"
      }
    },
    "query": "FROM elastic-cloud-logs-8\n| WHERE @timestamp >= NOW() - TO_TIMEDURATION(?lookback)\n| WHERE event.dataset == \"elasticsearch.server\"\n| WHERE log.level IN (\"ERROR\", \"WARN\")\n| WHERE elasticsearch.index.name LIKE ?index_pattern\n| WHERE (?cluster_name == \"\" OR elasticsearch.cluster.name == ?cluster_name)\n| STATS count = COUNT(*) BY bucket = DATE_TRUNC(5 minutes, @timestamp), elasticsearch.cluster.name, elasticsearch.index.name, log.level, error.type\n| SORT bucket DESC, count DESC\n| LIMIT 50"
  },
  "description": "Find ERROR and WARN level server log entries for the target index — surfaces indexing failures, engine errors, and document-level rejection messages.",
  "id": "app-index-triage-index_indexing_failures",
  "tags": [
    "app-index-triage",
    "application",
    "observability",
    "stack-monitoring"
  ],
  "type": "esql"
}
