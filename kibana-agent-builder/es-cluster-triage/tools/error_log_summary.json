{
  "configuration": {
    "params": {
      "cluster_name": {
        "description": "Cluster host-name prefix filter (e.g. 'es-prod'). Use an empty string to query all clusters.",
        "type": "string"
      },
      "lookback": {
        "description": "Lookback window as ES|QL time span text (e.g. 15 minutes, 1 hour, 24 hours).",
        "type": "string"
      },
      "node_name": {
        "description": "Optional node name filter; use an empty string to query all nodes.",
        "type": "string"
      }
    },
    "query": "FROM elastic-cloud-logs-8\n| WHERE @timestamp >= NOW() - TO_TIMEDURATION(?lookback)\n| WHERE log.level IN (\"ERROR\", \"WARN\")\n| WHERE event.dataset == \"elasticsearch.server\"\n| WHERE (?cluster_name == \"\" OR STARTS_WITH(host.name, ?cluster_name))\n| WHERE (?node_name == \"\" OR host.name == ?node_name)\n| KEEP @timestamp, log.level, host.name, message\n| SORT @timestamp DESC\n| LIMIT 50"
  },
  "description": "Summarize WARN and ERROR logs by node and message.",
  "id": "es-cluster-triage-error_log_summary",
  "tags": [
    "es-cluster-triage",
    "observability",
    "stack-monitoring"
  ],
  "type": "esql"
}