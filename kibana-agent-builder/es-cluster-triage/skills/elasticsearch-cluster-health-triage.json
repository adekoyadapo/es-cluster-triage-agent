{
  "content": "# ES Cluster Health Triage\n\nUse this skill when the issue looks cluster-wide:\n\n- red/yellow status\n- nodes missing or restarting\n- heap or CPU pressure\n- broad instability with no obvious single index\n- cluster-level saturation symptoms\n\n## Workflow\n\n1. Start with `es-cluster-triage-cluster_health_summary` to establish the cluster-wide symptom.\n2. If the cluster is degraded, use `es-cluster-triage-red_yellow_periods` to determine when the state changed.\n3. If node-level evidence is needed, use `es-cluster-triage-node_last_seen` and `es-cluster-triage-node_pressure_summary`.\n4. If heap or GC pressure is the likely cause, use `es-cluster-triage-jvm_gc_pressure`.\n5. If `red_yellow_periods` returns any non-green window, **always** call `es-cluster-triage-cluster_log_events` next.\n   Pass the affected cluster's host-name prefix as `cluster_name` and a `lookback` that covers the yellow/red window.\n   Look for `health status changed` lines — they contain the verbatim reason (e.g. shard started, node joined).\n   Also look for WARN/ERROR lines (OOM, allocation failure, circuit-breaker) in the same window.\n   Quote the reason in the report and use it to confirm or rule out root causes.\n6. Use `es-cluster-triage-error_log_summary` when you want a broader ERROR/WARN summary for the same window.\n7. **If the error log summary contains mapper errors, field-count warnings, or ILM failures** — invoke `es-cluster-triage-index-lifecycle-triage` to run `mapping_explosion_detection` and `ilm_stuck_indices`. Mapping explosion and ILM stalls are index-level root causes that surface during broad cluster-health sweeps.\n8. **If `node_pressure_summary` shows heap ≥ 80% on any node** — always run `es-cluster-triage-jvm_gc_pressure` next. Never report heap pressure without GC data; the old-gen collection frequency is the key signal that separates transient spikes from sustained memory pressure.\n9. Keep the output concise and explain whether the evidence suggests a cluster-wide issue or a narrower cause.\n\n## Output guidance\n\n- Name the affected cluster and node when available.\n- **Confidence gate for yellow/red events**: confidence may only be rated **High** when server logs\n  from `cluster_log_events` were queried for the anomaly window and corroborate the root cause.\n  If the index returned no data or the lookback did not cover the event, state this explicitly and\n  cap confidence at **Medium**.\n- **Heap confidence gate**: when heap ≥ 80% is reported, confidence may only be rated **High** for heap as a root cause when `es-cluster-triage-jvm_gc_pressure` has been run and shows old-gen GC activity or sustained heap > 85%. Without GC data, heap findings are **Medium** at best.\n- Call out monitoring coverage gaps explicitly.\n- Do not mention raw query text.\n- Remediation pointer: heap pressure → review GC logs and consider scaling node memory or reducing segment count; node loss → check server logs for OOM or network partition; broad rejections → investigate thread-pool queue sizing and reduce bulk batch sizes.\n",
  "description": "Diagnose broad Elasticsearch cluster health, red/yellow periods, node loss, heap pressure, CPU pressure, and general saturation symptoms.",
  "id": "es-cluster-triage-cluster-health-triage",
  "name": "ES Cluster Health Triage",
  "tool_ids": [
    "es-cluster-triage-cluster_health_summary",
    "es-cluster-triage-red_yellow_periods",
    "es-cluster-triage-node_last_seen",
    "es-cluster-triage-jvm_gc_pressure",
    "es-cluster-triage-cluster_log_events"
  ]
}
